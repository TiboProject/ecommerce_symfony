{% extends "base.html.twig" %}

{% block title %} Vérification d'email {% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="login-form bg-light mt-4 p-4">
                <h1 class="h3 mb-3 font-weight-normal">Vérifier votre email !</h1>
                <p>
                    Un email de vérification vous a été envoyé.
                    Veuillez cliquer sur le lien de vérification dans l'email pour activer votre compte.
                </p>
                <form action"{{path('app_resend_verify_email')}}" method="post">
                <button class="btn btn-primary">Renvoyer l'email</button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}